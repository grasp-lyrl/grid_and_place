# Co-emergence of grid cells and place cells.

## Setup

```bash
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -e .
```

Install pytorch following the instructions on https://pytorch.org/get-started/locally/.
```bash
pip3 install torch
```